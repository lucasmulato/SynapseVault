# SynapseVault UI (React + D3)

# Optional: override the API base URL.
# By default the UI uses a relative /api path, which Vite proxies to the
# gateway in dev and works out-of-the-box in Docker.
# VITE_API_URL=http://localhost:3000